import { View } from 'react-native';

export default function Separator(){
    return (
        <View style={{
            backgroundColor: '#ddd',
            height: 1,
            width: "100%",
        }}
        />
    );
};